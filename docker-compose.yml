version: '3.9'

services:
  backend:
   build:
      context: ./backend
   volumes:
      - ./backend:/app
   ports:
      - "5001:5001"

  frontend:
    build: ./frontend
    ports:
      - "8501:8501"
    volumes:
      - ./frontend:/app
    depends_on:
      - backend
